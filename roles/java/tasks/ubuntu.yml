- name: "Installing"
  block:
    - name: "Install the default version of OpenJDK from Ubuntu repository"
      apt:
        name: default-jre

  when: state == "started"

#
#- name: "Uninstalling"
#  block:
#    - name: "Uninstall Java"
#      apt:
#        pkg: default-jre
#        autoremove: yes
#        state: absent
#    - name: "Delete Debian repository"
#      file:
#        path: /etc/apt/sources.list.d/deb_debian_org_debian.list
#        force: true
#        state: absent
#      when: add_system_repo is defined
#  when: state == "absent"
