all:
  hosts:
    grafana:
      ansible_host: 192.168.215.15
  vars:
    ansible_user: ansible
    ansible_port: 22
    ansible_become: true

    platform: debian
    edition: ce

    features:
      - call
      - register
      - rtcp

    grafana:
      datasources:
        database: sip3
